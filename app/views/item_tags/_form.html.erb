<%= form_with(model: item_tag) do |form| %>
  <% if item_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(item_tag.errors.count, "error") %> prohibited this item_tag from being saved:</h2>

      <ul>
        <% item_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.text_field :item_id %>
  </div>

  <div>
    <%= form.label :tag_id, style: "display: block" %>
    <%= form.text_field :tag_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
