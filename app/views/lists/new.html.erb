<div class="main-container">
  <h2>Nova Lista</h2>

  <%= form_with(model: @list, local: true) do |form| %>
    <div class="form-group">
      <%= form.label :name, "Nome da Lista" %>
      <%= form.text_field :name, class: "form-control", placeholder: "Digite o nome da lista" %>
    </div>

    <div class="form-group">
      <%= form.label :category_id, "Categoria" %>
      <%= form.collection_select :category_id, Category.all, :id, :name, { prompt: "Selecione uma categoria" }, class: "form-control" %>
    </div>

    <div class="buttons-container">
      <%= link_to "Voltar", lists_path, class: "btn btn-secondary" %>
      <%= form.submit "Criar Lista", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
